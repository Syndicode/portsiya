const config = Object.freeze({
  templateVariables: Object.freeze({
    componentName: ["__DisplayName__"]
  }),
  paths: Object.freeze({
    components: ["./src/components", "./components"]
  })
});

module.exports = config;
